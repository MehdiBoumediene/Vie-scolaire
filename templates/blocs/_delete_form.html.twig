<form method="post" action="{{ path('app_blocs_delete', {'id': bloc.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bloc.id) }}">
    <button class="mb-2 mr-2 btn btn-danger" style="float:right;">Supprimer</button>
</form>
