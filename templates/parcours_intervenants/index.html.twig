{% extends 'base.html.twig' %}

{% block title %}Parcours index{% endblock %}

{% block body %}
    <h1>Parcours index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Bio</th>
                <th>ExperiencePro</th>
                <th>Competence</th>
                <th>Education</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for parcour in parcours %}
            <tr>
                <td>{{ parcour.id }}</td>
                <td>{{ parcour.bio }}</td>
                <td>{{ parcour.experiencePro }}</td>
                <td>{{ parcour.competence }}</td>
                <td>{{ parcour.education }}</td>
                <td>
                    <a href="{{ path('app_parcours_intervenants_show', {'id': parcour.id}) }}">show</a>
                    <a href="{{ path('app_parcours_intervenants_edit', {'id': parcour.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_parcours_intervenants_new') }}">Create new</a>
{% endblock %}
