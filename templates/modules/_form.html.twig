{{ form_start(form) }}
  
   <div class="form-row">
        <div class="col-md-6">
        {{ form_row(form.nom,{'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.nom) }}
        </div>
         <div class="col-md-6">
        {{ form_row(form.bloc,{'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.bloc) }}
        </div>

    </div>
    <br>
      <div class="form-row">
        <div class="col-md-6">
        {{ form_row(form.intervenants,{'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.intervenants) }}
        </div>
         <div class="col-md-6">
        {{ form_row(form.etudiants,{'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.etudiants) }}
        </div>

    </div>
 
 

    <br>
    <button class="mt-1 btn btn-primary">{{ button_label|default('Sauvegarder') }}</button> ou <a href="{{ path('app_modules_index') }}">Retour à la liste</a>
  
{{ form_end(form) }}
